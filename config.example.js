const config = {};

config.account = {
  username: 'x',
  password: 'x',
};

config.webdriver = {
  desiredCapabilities: {
    browserName: 'chrome',
  },
  width: 1280,
  height: 720,
};

export default config;
